name: Test the action
on: 
  push: 
    branches: ['main']

jobs: 
  build:
    runs-on: ubuntu-latest
    name: 'Local test'
    steps: 
      - name: Fetch Repo Code 
        uses: actions/checkout@v4
        
      - name: Build
        run: |
          npm i

      - name: Set Version 
        uses: ./ 
        id: calvergenerator
        with: 
          format: 'YYYYMMDD'
          prefix: 'v'
        